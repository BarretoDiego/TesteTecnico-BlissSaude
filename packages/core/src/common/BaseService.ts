/**
 * @module api/common/BaseService
 */

import { WithLogging } from "./WithLogging";

/** Base dos services de domínio: regras de negócio e orquestração. */
export abstract class BaseService extends WithLogging {}
