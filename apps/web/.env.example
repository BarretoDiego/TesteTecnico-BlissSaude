# =============================================================================
# apps/web — variáveis de ambiente
# Preenchidas automaticamente por scripts/localstack/export-outputs.sh após o deploy.
# =============================================================================

# Origem da API vista pelo **browser**.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/v1

# Origem vista pelo **servidor** Next. Iguais em desenvolvimento; diferentes em
# qualquer deploy conteinerizado, onde o servidor fala pela rede interna.
API_BASE_URL=http://localhost:4000/v1
